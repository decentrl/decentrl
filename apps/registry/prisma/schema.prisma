generator client {
  provider        = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model DidDocument {
  did         String   @id @unique
  didDocument Json
  signature   String
  version     Int @default(0)
}
